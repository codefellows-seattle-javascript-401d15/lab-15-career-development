'use strict';

const Stack = module.exports = require('./stack');

let bracketArray = []; //making an empty array

Stack.prototype.tinder = function(str) {
  if(str.startsWith(']' || '}' || ')') {
    
  }

  })
}
